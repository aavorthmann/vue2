apiVersion: v1alpha
suites:
  - name: "Chrome on Windows"
    browser: "chrome"
    platformName: "Windows 11"
    config:
      testFiles:
        [ "test/unit/**/*.*" ] 
kind: cypress
cypress:
  configFile: "tsconfig.json"
  version: "9.7.0"
region: "us-west"
